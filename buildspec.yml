version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
  build:
    commands:
      - pwd
      - cd slooky/
      - npm i
      - npm run build
      - npm run start
artifacts:
  files:
  - './dist/*'